[project]
name = "xuda-power-api"
version = "0.2.0"
description = "API for querying XZITC dorm power data by dorm number"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.12.3",
    "fastapi>=0.115.0",
    "requests>=2.32.3",
    "uvicorn>=0.34.0",
]

[project]
name = "netauth"
version = "0.1.0"
description = "Minimal login example for campus network self-service portal"
requires-python = "==3.12.*"
dependencies = [
    "requests>=2.32.5",
    "beautifulsoup4>=4.12.3",
]
